# תיקונים אוטומטיים

איך תיקון שאוהד שולח מגיע למאגר, ומה מונע ממנו להגיע לשם כשהוא לא
צריך. הכול רץ על שרתים של אחרים — GitHub ו-Google — ולכן ממשיך לעבוד
כשהמחשב שלך כבוי.

```
המשחק                Apps Script            GitHub Actions          Railway
─────                ───────────            ──────────────          ───────
טופס שדות סגורים  →  אימות + גיליון   →   כל שעה:               →  main מתעדכן
(שם / עמדה)          (fixes)               tour → guard → build      אחרי מיזוג
                                           → PR שממתין לך
טקסט חופשי        →  מייל אליך        →   אדם קורא. סוף.
(פידבק ובאגים)       (feedback)
```

---

## שני ערוצים, בכוונה

| | טופס התיקון | פידבק חופשי |
|---|---|---|
| מה נכנס | שדות מתוך רשימה סגורה | טקסט חופשי |
| לאן | גיליון → צינור אוטומטי → PR | מייל לתיבה שלך |
| מי מחליט | אתה, בלחיצת מיזוג | אתה, בקריאה |
| כתיבה למאגר | `config/names-<slug>.json` בלבד | אין |

ההפרדה הזאת היא כל העניין. **טקסט חופשי לא נכנס לשום צינור שכותב
נתונים.** מה שנכנס לצינור עובר אימות מול רשימה סגורה, ולכן אין בו מה
לפרש — יש בו מה לאמת.

הפידבק נשמר בגיליון `feedback` גם כשהמייל לא יוצא, ועמודת **מייל** שם
אומרת מה קרה לו: `נשלח`, `ללא כתובת` (אין `OWNER_MAIL`), `אין מכסה
להיום`, או שגיאה. כלומר: גיליון `feedback` ריק פירושו שאף אחד לא כתב.
תיבת דואר ריקה לבדה לא מוכיחה את זה.

---

## הטופס

נפתח מפאנל הסיום בלבד, על השחקן שהרגע נחשף. לפני סוף הסיבוב פתיחת
הטופס הייתה מגלה את התשובה.

שני שדות, ורק הם:

- **איות השם** — עברית בלבד, 2 עד 40 תווים
- **העמדה** — אחת מארבע: שוער, מגן, קשר, חלוץ

`src/engine.js`, סקציית "תיקון פרטי שחקן". התוספת של שדה שלישי היא
לא שינוי ב-UI — היא הרחבה של מה שאוטומט יכול לשנות במאגר, ודורשת
שינוי מתואם בארבעה מקומות: הטופס, `server/apps-script.gs`,
`tools/reports-apply.mjs`, ו-`tools/reports-guard.mjs`.

## הצינור

`tools/reports-apply.mjs` מושך את התור ומחיל מה שראוי. **אין בו מודל
שפה.** התור מכיל שדות מוקלדים, והחלטה על שדה מוקלד היא השוואה, לא
פרשנות.

תיקון מוחל רק אם כל אלה נכונים:

1. המועדון מוכר, השדה `he` או `pos`, והערך עובר אימות
2. השחקן קיים ב-`data/clubs/<slug>.json`
3. הערך שהמדווח ראה זהה למה שבמאגר עכשיו — דיווח על מצב שהשתנה מאז
   נדחה ולא דורס תיקון חדש יותר
4. שם מוצע שתפוס בידי שחקן אחר נדחה
5. **שלושה מדווחים נפרדים** על אותו תיקון בדיוק
6. לכל היותר **חמישה** תיקונים בריצה

לשינוי הספים: `--min=3 --max=5`, או `MIN_REPORTERS` ו-`MAX_PER_RUN`.

שורה שנפסלה על תוכן נסגרת לתמיד. שורה שרק טרם הצטברו לה מדווחים
נשארת בתור — אחרת המדווח השלישי שיגיע מחר לא היה מועיל לכלום.

## שומר הסף

`tools/reports-guard.mjs`. זה החלק שהוא הגנה ולא הבטחה: כל השאר הוא
קוד שאפשר שיהיה בו באג, והבדיקה כאן היא מה שקורה אם משהו למעלה נשבר.

- הקבצים שמותר לגעת בהם: `config/names-*.json`,
  `data/review/reports-seen.json`. שום דבר אחר, גם לא `src/`, גם לא
  ה-workflow עצמו.
- סכימה: מפתח הוא מספר, שם עברי, או שם לטיני לתעתיק. ערך הוא שם עברי
  או אובייקט עם `he`/`pos`/`nat`/`born`/`spells` בלבד.
- ב-CI (`GUARD_AUTO=1`) מותר לאוטומט לכתוב `he` ו-`pos` **בלבד**.
  `nat`, `born`, `spells` ומפתח תעתיק לטיני חוקיים בקובץ אבל רק מיד
  אדם. מחיקה אסורה תמיד.

השומר רץ **לפני** `enrich`, כי `enrich` משנה קבצים מיוצרים שאינם
ברשימה שלו.

## ומה שהוא לא מבטיח

- **`rid` הוא לא זהות.** מספר אקראי ב-localStorage. מי שמנקה אחסון
  מקבל חדש. הוא מסנן כפילויות ושובבות, לא תוקף נחוש.
- **אף הוראה בטקסט אינה גבול אבטחה.** מודל שקורא טקסט של תוקף אפשר
  לשכנע. לכן אין מודל בנתיב הכתיבה, וההגנה היא במשטח הכתיבה.
- **הערובה האחרונה היא אתה.** שום דבר לא מגיע ל-main בלי מיזוג. אם
  הכול למעלה ייכשל בבת אחת, מה שתראה הוא PR עם דיף של שורה אחת.

---

## הקמה — ארבעה דברים שרק אתה יכול לעשות

### 1. Apps Script

הגיליון הקיים → Extensions → Apps Script → להחליף הכול ב-
`server/apps-script.gs`. אחר כך Project Settings → Script properties:

```
OWNER_MAIL     = techbynoam@gmail.com
REPORTS_TOKEN  = <מחרוזת אקראית, 32 תווים ומעלה>
```

Deploy → Manage deployments → Edit → Version: **New** → Deploy.
`Execute as: Me`, `Who has access: Anyone`.

בדיקה: פתיחת ה-URL בדפדפן מחזירה
`{"ok":true,"msg":"sportdle collector is running"}`.

הגרסה הקודמת של הסקריפט קיבלה דיווחים ב-`type:"report"` ורשמה מהם רק
`type/puzzle/guesses/won` — הטקסט של כל דיווח שנשלח עד היום נזרק בשקט.
זה תוקן.

### 2. `config/site.json`

```json
"analyticsUrl": "https://script.google.com/macros/s/…/exec"
```

כל עוד השדה ריק: הטופס אומר שהשליחה לא מוגדרת, והפידבק נופל למייל
דרך `mailto:`. שום דבר לא נשבר — פשוט אין תור.

### 3. המקור ל-GitHub

נעשה ב-26/08/2026: המקור יושב ב-`noamedery1/sportdle` (ציבורי), והמאגר
`noamedery1/beitardle` הוא מאגר **הפריסה** — קבצים סטטיים בלבד. הצינור
צריך את המקור, כי הוא מריץ `enrich` ו-`build`.

מאגר פרטי שורף דקות Actions; ריצה שעתית של הצינור היא בערך 24 דקות
ביום. אם זה מציק — `cron: "17 */3 * * *"`. במאגר ציבורי זה חינם.

### 4. סודות ב-Actions

במאגר החדש, Settings → Secrets and variables → Actions:

| סוד | מה זה |
|---|---|
| `REPORTS_URL` | אותו `/exec` |
| `REPORTS_TOKEN` | אותו טוקן מ-Script properties |
| `DEPLOY_TOKEN` | Fine-grained PAT, `contents: write`, למאגר הפריסה בלבד |

שני מלכודות שכל אחת מהן מפילה את הצינור בשלב אחר, ואף אחת מהן אינה סוד:

1. **Settings → Actions → General → Workflow permissions** → לסמן
   **Allow GitHub Actions to create and approve pull requests**. בלי זה
   הענף נדחף והצינור נופל על `gh pr create` בהודעה
   `GitHub Actions is not permitted to create or approve pull requests`.
   ה-`permissions:` שב-workflow לא עוקף את זה — זו חסימה נפרדת.
2. **Script properties הם המקום שבו הטוקן חי, לא הסוד בגיטהאב לבדו.**
   `checkToken_` מחזיר false כשהתכונה חסרה, ולכן תור שלא הוגדר לו טוקן
   נראה בדיוק כמו תור עם טוקן שגוי: `אין הרשאה`. אותו דבר עם
   `OWNER_MAIL` — בלעדיו אין מייל, ובלי עמודת "מייל" בגיליון גם אין לזה
   עקבות.

---

## הפעלה ובדיקה

- **ריצה יבשה:** Actions → "תיקונים מהטופס" → Run workflow → `dry`
  מסומן. מדפיס מה היה קורה, בלי לכתוב ובלי PR.
- **מקומית:** `REPORTS_URL=… REPORTS_TOKEN=… node tools/reports-apply.mjs --dry`
- **וטו על שורה:** בגיליון, בעמודת "מצב", לכתוב כל דבר. שורה עם מצב
  לא ריק לא חוזרת בתור.
- **הקפאת הצינור:** להסיר את `REPORTS_URL` מהסודות. הריצה תדלג בשקט
  (יציאה נקייה, בלי מייל כישלון) ולא תיגע בכלום.

## כשריצה נכשלת

מייל הכישלון של גיטהאב מקשר לעמוד הריצה. שם, בראש העמוד, מופיעה
ההודעה שמסבירה מה לתקן — הצינור כותב אותה ל-Job summary ולאנוטציה,
בדיוק כדי שלא יהיה צריך לפתוח לוגים.

| מה שכתוב | מה לתקן |
|---|---|
| השרת דחה את הטוקן | `REPORTS_TOKEN` בסודות Actions ≠ ה-Script property באותו שם. תו-בתו, כולל רווח מודבק |
| השרת לא החזיר JSON | הפריסה של Apps Script אינה `Who has access: Anyone` — מה שחזר הוא דף התחברות של גוגל |
| התשובה בלי שדה rows | הפריסה היא גרסה ישנה של הסקריפט. Deploy → Manage deployments → Edit → Version: **New** |
| ל-DEPLOY_TOKEN אין הרשאת כתיבה | בטוקן העדין: Repository permissions → Contents → **Read and write**, ו-`beitardle` ב-Repository access |
| שומר הסף עצר | הריצה נגעה בקובץ שאינו `config/names-*.json` או בשדה שאינו `he`/`pos`. זה לא באג בשומר |

מאגר הפריסה ציבורי, ולכן `checkout` שלו מצליח **גם** עם טוקן פג-תוקף.
לכן `tools/token-check.mjs` בודק את הרשאת הכתיבה לפני העבודה, ולא
מחכה שהדחיפה תיפול בסוף.

## מה שהצינור לא עושה

תיקון לשחקן שאינו במאגר, הוספת שחקן, שנת לידה, לאום, עונות, כל שינוי
בקוד. אלה מגיעים בפידבק חופשי, במייל, ואדם מטפל בהם — כי הם דורשים
שיקול דעת מול מקורות, וזה בדיוק מה שאין לו ייצוג כ-enum.
