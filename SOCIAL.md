# הפוסט היומי

איך המשחק מפרסם את עצמו כל בוקר, בלי שהמחשב שלך דלוק.

```
GitHub Actions            tools/social.mjs         עמוד פייסבוק
──────────────            ────────────────         ────────────
כל יום 05:10 UTC     →    מרכיב את הפוסט      →    Graph API
(08:10 בקיץ)              מהלוחות שבמאגר           ↘ ערוץ טלגרם

יום ראשון בנוסף:    →    tools/clip.mjs      →    סרטון בפוסט
                          מקליט משחק אמיתי         + artifact לטיקטוק
```

---

## מה מתפרסם, ולמה דווקא זה

**התשובות של אתמול, וקישור לחידה של היום.** לא רמז ולא שאלה על
החידה הנוכחית.

זו לא החלטה עיצובית אלא חשבון פשוט: רמז על החידה שבאוויר משנה
אותה לכל מי שעוד לא שיחק. פוסט כזה עובד פעם אחת, ואז אנשים
מפסיקים לעקוב כדי לא להתקלקל. תשובות של אתמול נותנות **סגירה**
למי ששיחק, **סקרנות** למי שלא, ואפס נזק.

הפוסט נראה כך:

```
SportDle · חידה #34

התשובות של אתמול (#33):
ביתרdle — אלי אוחנה (81/82)
באר־שבעdle — אלעד גבאי (12/13)
מכביdle — אבי נמני (89/90)
חיפהdle — אלון חרזי (90/91)
הפועלdle — יוסי אבוקסיס (87/88)

החידה של היום כבר באוויר. שמונה ניסיונות.
חמישה מועדונים, לכל אחד מאגר ולוח משלו.

https://sportdle.up.railway.app/sportdle/
```

לבדיקה בלי לפרסם:

```bash
node tools/social.mjs
node tools/social.mjs --day=40      # איך ייראה הפוסט של חידה 40
```

---

## הסרטון

```bash
node tools/clip.mjs                  # קליפ להיום
node tools/clip.mjs --club=beitar --puzzle=12 --out=x.mp4
```

מקליט **משחק אמיתי** מהאתר החי: הקלדה תו־תו, רשימת ההצעות
נפתחת, אריחים נכנסים באנימציה, ניצחון. 1080×1920, בתוך מסגרת
טלפון, כ-25 שניות.

**הוא לעולם לא מראה את החידה של היום** — הוא משחק חידת ארכיון
מלפני שבוע. המועדון מתחלף לפי מספר החידה, כך שחמישה ימים ברצף
נותנים חמישה מועדונים.

שלושה דברים מונעים את קפיצת המסך שהופכת הקלטה כזאת לבלתי צפייה:
`focus({preventScroll})`, הקלדת שם מלא ו-Enter במקום לחיצה על
הצעה, ומאזין שמצמיד את הגלילה לראש. הכותרת נשארת בפריים לכל
אורך המשחק.

דורש `ffmpeg` ב-PATH. על ubuntu-latest הוא מותקן מראש.

**טיקטוק — ידני.** ה-API של טיקטוק לפרסום דורש אישור שותפים
ואינו זמין לפרויקט קטן. לכן הסרטון נשמר גם כ-artifact של הריצה:
נכנסים ל-Actions → הריצה → `sportdle-clip`, מורידים, ומעלים.
שלושים שניות ביום ראשון.

---

## מה צריך להגדיר, פעם אחת

### פייסבוק

הצעד הזה הוא היחיד שאני לא יכול לעשות בשבילך — הוא דורש כניסה
לחשבון שלך.

1. **[developers.facebook.com](https://developers.facebook.com)** → My Apps
   → Create App → סוג **Business** → שם כלשהו.
2. באפליקציה: Add Product → **Facebook Login for Business** (מספיק
   שהוא קיים; לא צריך להגדיר אותו).
3. **[Graph API Explorer](https://developers.facebook.com/tools/explorer/)**:
   * בוחרים את האפליקציה שיצרת
   * User or Page → **Get Page Access Token** → בוחרים את העמוד
   * Permissions: `pages_manage_posts` ו-`pages_read_engagement`
   * Generate Access Token, מאשרים
4. הטוקן שקיבלת קצר-טווח (שעה). להארכה:
   **[Access Token Debugger](https://developers.facebook.com/tools/debug/accesstoken/)**
   → מדביקים → **Extend Access Token** → מקבלים טוקן ארוך-טווח.
   טוקן **עמוד** ארוך-טווח לא פג כלל, כל עוד הסיסמה לא משתנה
   ולא מבטלים את ההרשאה.
5. **מזהה העמוד**: בעמוד → About → Page ID. או ב-Graph API
   Explorer: `me/accounts`.

**מה שחוסם אנשים בשלב הזה:** ההרשאות `pages_manage_posts`
דורשות App Review — **אבל רק כשאפליקציה משמשת אחרים.** כל עוד
אתה מנהל האפליקציה **וגם** מנהל העמוד, זה עובד במצב Development
בלי שום אישור. אל תשלח לבדיקה.

### טלגרם, אם תרצה

1. שיחה עם [@BotFather](https://t.me/BotFather) → `/newbot` → מקבלים טוקן.
2. יוצרים ערוץ, מוסיפים את הבוט כמנהל עם הרשאת פרסום.
3. מזהה הערוץ: שולחים הודעה בערוץ ואז פותחים
   `https://api.telegram.org/bot<TOKEN>/getUpdates` — המזהה הוא
   מספר שלילי, למשל `-1001234567890`.

### הסודות ב-GitHub

`Settings → Secrets and variables → Actions → New repository secret`:

| שם | מה |
|---|---|
| `FB_PAGE_ID` | מזהה העמוד |
| `FB_PAGE_TOKEN` | ה-Page Access Token הארוך |
| `TG_BOT_TOKEN` | אופציונלי |
| `TG_CHAT_ID` | אופציונלי |

**חסר סוד? הערוץ מדולג בשקט והריצה מסתיימת בהצלחה.** זה מכוון:
cron שנכשל כל יום הופך לרעש שמפסיקים להסתכל עליו, וביום שהכישלון
יהיה אמיתי אף אחד לא ישים לב.

---

## הרצה ראשונה

אחרי שהסודות מוגדרים:

`Actions → פוסט יומי → Run workflow` — ולסמן **dry** בפעם
הראשונה. זה מדפיס את הפוסט בלוג בלי לפרסם, ומאפשר לראות שהכל
מורכב נכון. אחר כך אותו דבר בלי dry.

לבדיקת הסרטון: אותו מקום, מסמנים **video**. הריצה תיקח כארבע
דקות — רובן התקנת הדפדפן.
