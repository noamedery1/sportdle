# האפליקציה

איך אותו משחק שרץ באתר הופך לאפליקציה בחנויות, בלי גרסה שנייה
לתחזק.

```
src/  →  build.mjs  →  dist/  ─┬─→  האתר (Railway)
                               └─→  cap sync  →  android/  →  APK / AAB
                                                  ios/     →  IPA
```

`dist` הוא `webDir` של Capacitor. **אין קוד אפליקציה נפרד** —
אותו מנוע, אותו CSS, אותם 561 דפים. מה שנוסף הוא `src/native.js`,
שיוצא מיד כשאין Capacitor.

---

## למה Capacitor ולא Flutter או React Native

מדדנו לפני שהחלטנו: `engine.js` הוא 1,197 שורות, מהן **820
לוגיקה טהורה**; `versus.js` עוד 925; ו-463 שורות CSS. שכתוב
ב-Flutter היה זורק את כל זה ומתחיל מ-Dart. React Native היה
משמר את הלוגיקה אבל זורק את ה-UI.

Capacitor משמר **את הכל**, ובתמורה צריך להוכיח לאפל שזו לא עטיפה
דקה של אתר — ראה למטה.

---

## מה עושה `src/native.js`

חמישה דברים, וכולם דברים שדפדפן לא יכול. זה לא קוסמטיקה: **כלל
4.2 של אפל דוחה אתר עטוף**, וזו הסיבה שהם קיימים.

| מה | פרט |
|---|---|
| **התראה יומית** | מקומית, לא Push. אין שרת, אין FCM, אין טוקנים |
| **רטט** | על כל אריח. חזק יותר כשהאריח מדויק |
| **גיליון שיתוף** | של המערכת, במקום העתקה ללוח |
| **כפתור חזור** | סוגר חלונית במקום לצאת מהאפליקציה |
| **מסך פתיחה** | נסגר כשהגופנים נטענו, לא לפני |

**ההרשאה להתראות נדרשת רק אחרי החידה הראשונה.** בקשה קרה
בשנייה הראשונה נדחית ברוב המקרים, ואחרי דחייה אין דרך חזרה בלי
לשלוח את המשתמש להגדרות המערכת. אחרי שפתרת חידה, "להזכיר לך
מחר?" הוא בדיוק מה שמצפים לו.

**`allowWhileIdle` כבוי בכוונה.** הוא מתרגם לאזעקה מדויקת, שדורשת
`SCHEDULE_EXACT_ALARM` — הרשאה שגוגל מצפה שתידרש רק כשתזמון
מדויק הוא ליבת האפליקציה. תזכורת שמגיעה 8:30 או 8:41 היא אותה
תזכורת, ואין סיבה להזמין שאלות בביקורת.

---

## שלושה דברים שאסור לגעת בהם

**`androidScheme` ו-`hostname` ב-`capacitor.config.json`.** הם
קובעים את ה-origin שממנו ה-WebView מגיש, ו-`localStorage` שייך
ל-origin. שינוי שלהם בעדכון עתידי **מוחק לכל שחקן את הרצף
והסטטיסטיקה**, בשקט.

**`appId`** — `sportdle.techbynoam.com`. אי אפשר לשנות אותו בחנות
אחרי הפרסום הראשון.

**הגופנים המקומיים.** `build.mjs` מפיל בנייה שמחזירה קישור
ל-Google Fonts. בלי גופן מקומי האפליקציה נראית שבורה אופליין,
וטיעון האופליין מול 4.2 נחלש.

---

## הרצה

```bash
node tools/fonts.mjs      # פעם אחת, או כשמשנים משקלים
node tools/appicons.mjs   # אייקונים ומסך פתיחה מצבעי המועדונים
npx @capacitor/assets generate --android
node scripts/build.mjs
npx cap sync android
```

**APK להתקנה בטלפון** נבנה ב-GitHub Actions ולא מקומית, כי
במחשב הפיתוח אין Java, אין Android SDK ואין Gradle:

`Actions → APK → הריצה → artifacts → sportdle-apk`

זה APK **debug**. להתקנה צריך לאשר "התקנה ממקורות לא מזוהים"
באנדרואיד. לחנות צריך AAB חתום במפתח release — שלב נפרד.

### iOS

GitHub Actions מריץ ראנרים של macOS, ולכן הוא **יכול** לקמפל.
מה שהוא לא יכול בלי חשבון מפתח אפל בתשלום הוא לחתום: `.ipa`
להתקנה על מכשיר דורש תעודה ופרופיל, ואלה נכנסים כסודות של
המאגר. בלי חתימה אפשר לבנות לסימולטור בלבד.

---

## iOS בלי מק

**אין שום שלב שדורש מק.** הנקודה שנראית חוסמת — יצירת תעודת
החתימה — נפתרת ב-OpenSSL, ואת הבנייה עושה ראנר macOS ב-Actions.

| שלב | איפה | מק? |
|---|---|---|
| הרשמה ל-Apple Developer Program (99$/שנה) | דפדפן | לא |
| יצירת CSR | `openssl` על Windows | לא |
| Distribution certificate | developer.apple.com | לא |
| Provisioning profile | developer.apple.com | לא |
| המרה ל-`.p12` | `openssl` | לא |
| בנייה וחתימה של `.ipa` | ראנר `macos-latest` | לא |
| העלאה ל-App Store Connect | `xcrun altool` על הראנר | לא |
| דף החנות, פרטיות, דירוג גיל, הגשה | דפדפן | לא |
| **בדיקה על מכשיר** | **צריך אייפון** | — |

**ה-CSR בלי מק** (זה מה שנראה כמו מחסום ואינו):
```
openssl genrsa -out ios_dist.key 2048
openssl req -new -key ios_dist.key -out ios_dist.csr   -subj "/emailAddress=techbynoam@gmail.com/CN=TechByNoam/C=IL"
```
מעלים את ה-`.csr` ל-developer.apple.com, מורידים `ios_distribution.cer`,
וממירים:
```
openssl x509 -in ios_distribution.cer -inform DER -out dist.pem -outform PEM
openssl pkcs12 -export -inkey ios_dist.key -in dist.pem -out ios_dist.p12
```
ה-`.p12` והסיסמה הם שני הסודות שנכנסים ל-GitHub, בדיוק כמו
ה-keystore של אנדרואיד.

**להעלאה מה-CI עדיף App Store Connect API key** (Issuer ID,
Key ID וקובץ `.p8`) ולא Apple ID עם סיסמה: הוא לא נשבר על
אימות דו-שלבי ולא פג בכל שינוי סיסמה.

**מה שכן דורש מכשיר iOS:** בדיקה אמיתית. שלוש דרכים בלי לקנות
אייפון —
1. **TestFlight אצל חבר** — מעלים ל-TestFlight מה-CI, החבר
   מתקין ובודק. חינם, וזו הדרך הטובה ביותר.
2. **חוות מכשירים בענן** (BrowserStack App Live וכדומה) —
   מתקינים `.ipa` על מכשיר אמיתי מהדפדפן. בתשלום, זול ליום.
3. **הסימולטור ב-CI** — `ios.yml` מאתחל סימולטור, מתקין,
   מריץ ומצלם ל-artifact `sportdle-ios-sim`. **תופס** פריסה
   שנשברה, נכס חסר ושגיאת JS. **לא תופס** מקלדת, רטט, גיליון
   שיתוף או התראות — כלומר לא את מה שכלל 4.2 בודק.

**⚠ חסר ל-Universal Links:** אנדרואיד עובד דרך
`/.well-known/assetlinks.json`, שקיים. **לאפל צריך
`/.well-known/apple-app-site-association`**, והוא **לא נוצר
עדיין** — הוא דורש את ה-Team ID מהחשבון בתשלום. בלעדיו קישורי
הזמנה ושיתוף ייפתחו בדפדפן ולא באפליקציה ב-iOS.

## לפני הגשה לחנויות

- [ ] **גוגל:** חשבון אישי חדש נדרש בדיקה סגורה עם 12 בודקים
      במשך 14 יום לפני פרסום לייצור. להתחיל לגייס **במקביל**
      לפיתוח, לא אחריו.
- [ ] **אפל:** 99$ לשנה. **גוגל:** 25$ חד-פעמי.
- [ ] **פרסומות:** AdSense אסור בתוך אפליקציה — לאפליקציה
      **AdMob**. הרצת AdSense ב-WebView מסכנת את חשבון ה-AdSense
      עצמו. שני שילובים נפרדים.
- [ ] **ATT ב-iOS** אם יוצגו פרסומות מותאמות.
- [ ] **שם בחנות: SportDle**, לא "ביתרdle". שמות מועדונים בתיאור
      בלבד, כאזכור עובדתי, עם ההצהרה "פרויקט אוהדים · לא רשמי"
      במקום בולט. בחנות יש כפתור תלונת קניין רוחני, והחנויות
      מסירות קודם ושואלות אחר כך. **אין בפרויקט שום סמל מועדון**,
      וזה מה שמוריד את הסיכון.
- [ ] טפסי **Data safety** (גוגל) ו-**App Privacy** (אפל): לסמן
      אחסון מקומי, מדידת שימוש אנונימית, ו-Firebase ללשונית הקרב.

---

## מה שנשאר לבדוק על מכשיר אמיתי

הדברים האלה עובדים בכל דפדפן ונשברים לפעמים ב-WebView. אין דרך
לאמת אותם בלי טלפון:

1. **לשונית הקרב** — ה-SDK של Firebase ב-WebView, בשני המכשירים.
2. **`wa.me`** — צריך להיפתח בוואטסאפ ולא בתוך האפליקציה.
3. **ההתראה** — לאשר, לסגור, ולבדוק שהיא מגיעה בבוקר.
4. **הרצף** — לשחק, לסגור את האפליקציה לגמרי, לפתוח, ולוודא
   שהניחושים והרצף שם.
5. **אופליין** — מצב טיסה, ולוודא שהמשחק והגופנים תקינים.
