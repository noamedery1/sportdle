# מה חסר, ובאיזה סדר

מצב נכון ל-26.8.2026. המספרים מ-`node tools/gaps.mjs`.

| מועדון | טווח | שחקנים | בבריכה | חסומים | בלי שם עברי | עונות חסרות |
|---|---|---|---|---|---|---|
| ביתרדל | 69/70–25/26 | 410 | **121** | 0 | 0 | 0 |
| באר־דל | 72/73–25/26 | 419 | **81** | 21 | 20 | 4 |
| מכבידל | 69/70–25/26 | 359 | **111** | 13 | 49 | 0 |
| חיפהדל | 73/74–25/26 | 390 | **104** | 13 | 43 | 4 |
| הפועלדל | 69/70–25/26 | 442 | **90** | 12 | 51 | 0 |

המספרים זזים כל עוד `enbridge` רץ. המקור החי הוא `node tools/gaps.mjs`.

* **חסום** — במאגר, אבל בלי עמדה או שנת לידה, אז לא יכול להיות תשובה.
* **בלי שם עברי** — worldfootball מכיר אותו, אין גשר לעברית, לא במאגר בכלל.
  נספרים רק מי ששיחק 3 עונות ומעלה.
* **מוסתר** — בלי עמדה **וגם** בלי שנת לידה. הוא לא מוצג במשחק בכלל:
  ניחוש עליו החזיר שורה של סימני שאלה, וזה נראה כמו תקלה. הרשימה
  המלאה, עם המפתח לתיקון לכל שחקן: `node tools/hidden.mjs` →
  `data/review/hidden.md`.

---

## אילו אתרים לא נמשכו

**אחד. worldfootball — וגם הוא נפתר בסוף.**

| מקור | סטטוס | מה נמשך |
|---|---|---|
| worldfootball | ✔ נפתר | 57 עונות × 5 מועדונים, 1969/70–2025/26 |
| ההתאחדות לכדורגל | ✔ | 24 עונות × 5, 2002/03–2025/26 |
| ויקיפדיה — קטגוריות | ✔ | 380–430 ערכים למועדון |
| ויקיפדיה — תיבות מידע | ✔ | עמדה ל-97%, שנת לידה ל-98% |
| ויקיפדיה — קישורי אנגלית | ✔ | 83% מהערכים |
| ויקיפדיה האנגלית — גשר | ✔ | לכתיב שלא תואם |

### מה קרה עם worldfootball

מ-Playwright הוא נחסם: דפי `vs<Y>-<Y+1>/squad/` מאחורי אתגר Cloudflare,
הבקשה הראשונה עוברת והשאר מקבלות 403 קבוע. **לא נעשה ניסיון לעקוף
זיהוי בוטים.**

מה שכן עבד: בדפדפן שכבר עבר את האתגר, `fetch` רגיל מאותו origin
לכתובות העונות מחזיר 200 עם התוכן הנכון. זה בדיוק מה שדפדפן עושה
כשלוחצים על תפריט העונות — בלי זיוף, בלי CAPTCHA, עם השהיה של
1.1 שניות בין בקשות.

**זה מה שרץ בפועל, וזה מה שיש ב-`data/urls/<slug>-wf-console.js`
כדי שתוכל לחזור על זה בעצמך.**

בדיקת האימות: worldfootball החזיר **408 שחקני בית"ר** מול **410**
במאגר הייצור. זו בדיוק בדיקת השפיות שסעיף 7 במפרט ביקש.

---

## הרצה ידנית — לינק וסקריפט לכל אתר ולכל מועדון

```bash
node tools/urls.mjs            # מייצר הכול
node tools/urls.mjs maccabi-ta # מועדון אחד
```

נוצר ב-`data/urls/`:

| קובץ | מה זה |
|---|---|
| `INDEX.md` | אינדקס עם כל הלינקים |
| `<slug>-wf.txt` | 57 כתובות worldfootball |
| `<slug>-ifa.txt` | 24 כתובות ההתאחדות |
| `<slug>-wiki.txt` | כתובת ה-API של קטגוריית ויקיפדיה |
| `<slug>-wf-console.js` | סקריפט להדבקה בקונסולת הדפדפן |

### הדרך שעובדת ל-worldfootball

1. פתח בדפדפן שלך `https://www.worldfootball.net/teams/<wf>/squad/`
   וחכה שהדף ייטען באמת (לא "Just a moment").
2. F12 → Console → הדבק את `data/urls/<slug>-wf-console.js`.
3. ~2 דקות למועדון. לעצור באמצע: `stop = true`.
4. בסוף יורד `<slug>-worldfootball.json`. שים אותו ב-`data/raw/`.

**אם כרום חוסם הורדה חוזרת** (זה קורה אחרי כמה קבצים מאותו דף) —
הרם את השרת המקומי ושלח ישירות:

```bash
npm run serve
```

ואז בקונסולה, במקום ההורדה:

```js
await fetch("http://localhost:4173/ingest/<slug>-worldfootball.json",
  { method:"POST", headers:{"Content-Type":"text/plain"},
    body: JSON.stringify(data) });
```

השרת מקבל גם `gzip+base64` עם הכותרת `X-Encoding: gzip-base64` —
88KB הופכים ל-14KB. שים לב: דף HTTPS לא יכול לפנות ל-HTTP מקומי
(חסימת תוכן מעורב), אז זה עובד רק מדף שמוגש מלוקאלהוסט.

### ההתאחדות וויקיפדיה

שניהם עובדים ישירות מהסקריפט, בלי טריקים:

```bash
node scripts/scrape.mjs --source=ifa                # דורש דפדפן, ~6 דקות
node scripts/scrape.mjs --source=wiki,wikiplayers,wikiextra,wikilang
node scripts/scrape.mjs --source=enbridge           # איטי, אבל המשכי
```

`enbridge` שומר כל עשרה חיפושים וזוכר את מי שכבר נבדק. אפשר לעצור
באמצע (Ctrl+C) ולהריץ שוב — הוא ימשיך מאיפה שהפסיק.

הכול נשמר ל-`data/raw/html/`, ו-`--reparse` בונה מחדש בלי רשת.

---

## מה עוד חסר

### 1. שם עברי ל-163 שחקנים

worldfootball מכיר אותם, אין להם גשר לעברית. אלה עם 3+ עונות —
כלומר מועמדים אמיתיים לבריכה.

למה אין גשר: או שאין להם ערך ויקיפדיה בכלל, או שהכתיב הלטיני שונה
מדי משני הצדדים גם אחרי החיפוש באנגלית.

```bash
node tools/gaps.mjs maccabi-ta
```

מדפיס שורות מוכנות להעתקה:

```json
"Avi Nimny": "אבי נימני",
```

ל-`config/names-<slug>.json`. תעתיק ידני הוא בדיוק מה שסעיף 4
במפרט מבקש — הצינור לא ינחש אותו לבד.

### 2. עמדה ושנת לידה ל-59 שחקנים

במאגר, אבל חסרות תכונות. אותו קובץ, מפתח לפי `player_id`:

```json
"49494": { "pos": "DF", "born": 1991 },
```

`node tools/gaps.mjs <slug>` מדפיס גם אותם, עם ה-`player_id` הנכון.

### 3. שלוש כפילויות שם

טל בן חיים כבר סגור (בלם 1982 / חלוץ 1989). נשארו:

| מועדון | שם | תקופה א' | תקופה ב' |
|---|---|---|---|
| מכבי חיפה | מוחמד גדיר | 10/11–11/12 | 20/21 |
| הפועל ת"א | רועי דיין | 07/08 | 16/17 |
| הפועל ת"א | עמנואל בואטנג | 18/19–20/21 | 25/26 |

כרגע כל אחד מופיע פעמיים עם הבהרה `(עונה ראשונה)`. אם זה אותו אדם —
צריך למזג, והתקופות וספירת התארים משתנות.

**וגם: שני "גיא מלמד".** worldfootball מחזיק שניים — מגן יליד 1979
(97/98–99/00 במכבי חיפה) וחלוץ יליד 1992 (24/25–25/26). הצינור סירב
לשייך תכונות לאף אחד מהם, אז שניהם בלי עמדה ושנת לידה ולא בבריכה.
להוסיף ב-`config/names-maccabi-haifa.json` לפי `player_id` של ההתאחדות.

### 4. נכסים גרפיים

הדף מקשר אליהם, הם לא קיימים, והבנייה מזהירה:

`favicon.ico` · `icon-32.png` · `icon-180.png` · `icon-512.png` ·
**`og.png`** (1200×630 — צריך חדש לחמישה מועדונים, לא זה של ביתרדל)

מניחים ב-`src/static/`. `manifest.json` נוצר אוטומטית.

### 5. הגדרות

| שדה ב-`config/site.json` | נוכחי | מה צריך |
|---|---|---|
| `siteUrl` | `sportsdel.techbynoam.com` | הדומיין הקנוני |
| `analyticsUrl` | ריק (איסוף כבוי) | ה-Web App מ-Apps Script |
| `start` | `[2026, 8, 24]` | **לאמת מול ביתרדל שבאוויר** — זה תאריך חידה #1, וטעות בו מזיזה את כל הארכיון |

---

## סדר פעולות

**עכשיו — אפשר לעלות:**

1. `og.png` והאייקונים ל-`src/static/`.
2. `siteUrl`, ואימות `start` מול הייצור.
3. `npm run build`.

121/81/111/104/90 חידות. **81 יום למועדון החלש ביותר.**

**כשיהיה לך זמן:**

4. `node tools/gaps.mjs <slug>` לכל מועדון → למלא תעתיקים ותכונות
   ב-`config/names-<slug>.json`.
5. לענות על שלוש הכפילויות.
6. `npm run enrich && npm run build`.

כל תוספת נכנסת **בסוף** הלוח. חידה שכבר פורסמה לא זזה — זה נבדק
על בית"ר: 119 החידות של הייצור נשארו זהות בייט-בייט.

**אם בונים לוח מחדש למועדון שכבר באוויר** — `enrich` ייפול בכוונה
אם שם שבלוח נעלם מהמאגר. זו החלטה אנושית, לא משהו לתקן בשקט.

---

## מה לא חסר

* `worldfootball` ו-`ifaTeamId` לחמשת המועדונים — מאומתים מול המקור.
* רשימות האליפויות והגביעים — נגזרות ב-`tools/parse-titles.mjs`,
  שנופל אם הוא לא משחזר שנה־בשנה את בית"ר ואת הפועל ב"ש ואם הסכומים
  לא תואמים לספירות של הכתבה. עובר.
* המנוע, הבורר, הצבעים, בידוד ה-`localStorage`, תשע בדיקות הבנייה —
  כולל בדיקה שכל אחת מהן באמת מפילה את הבנייה.
