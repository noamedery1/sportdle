<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: קישור הזמנה לחדר נפתח באפליקציה ולא בדפדפן.
                 autoVerify מחייב את /.well-known/assetlinks.json באתר,
                 עם טביעת האצבע של התעודה שחתמה על האפליקציה המותקנת.
                 בלי הקובץ הזה האימות נכשל והקישור ייפתח בדפדפן.

                 **נתיבים מפורשים, ולא כל האתר.** intent-filter אינו יכול
                 להתאים לפי query string, ולכן התאמה על "/" הייתה חוטפת
                 גם את העמוד הראשי וגם כל קישור לדף תוכן — מי שמשתף דף
                 שחקן היה פותח את המשחק במקום את הדף.

                 /join     — הזמנה לחדר קרב
                 /<slug>/  — שיתוף הפתרון היומי. חמשת המועדונים, וזה
                             גם הדף שהאתר מגיש ממילא.

                 **מפתח אחד בלבד, ולא גם הדומיין הישן.** אנדרואיד מאמת
                 כל מפתח שמוצהר, ואם אחד נכשל — **כל** ה-App Links של
                 האפליקציה נכשלים. הדומיין הישן מופנה ב-301 ולכן לא
                 יוכל להגיש assetlinks.json, ולהצהיר עליו היה מפיל את
                 האימות כולו. קישורים ישנים ימשיכו לעבוד בדפדפן. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="sportdle.techbynoam.com"
                      android:pathPrefix="/join" />
                <data android:scheme="https" android:host="sportdle.techbynoam.com"
                      android:pathPrefix="/beitar" />
                <data android:scheme="https" android:host="sportdle.techbynoam.com"
                      android:pathPrefix="/hapoel-bs" />
                <data android:scheme="https" android:host="sportdle.techbynoam.com"
                      android:pathPrefix="/hapoel-ta" />
                <data android:scheme="https" android:host="sportdle.techbynoam.com"
                      android:pathPrefix="/maccabi-haifa" />
                <data android:scheme="https" android:host="sportdle.techbynoam.com"
                      android:pathPrefix="/maccabi-ta" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
