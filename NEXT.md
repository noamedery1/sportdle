# הדרך לחנות — מה נשאר

עודכן 02/09/2026. `main` ו-`app-capacitor` זהים.

**הזהות אומתה והאפליקציה נוצרה.** חשבון `TechByNoam`, מזהה
`8750398662776521018`; רשומת האפליקציה `4974294682160246747`,
חבילה `com.techbynoam.sportdle`, עברית כברירת מחדל, משחק, חינם.

**נותרו ארבעה סעיפים: דף חנות, Data safety, דירוג תוכן, והעלאת
ה-AAB.** אחריהם 12 בודקים × 14 יום, וזה כל מה שקובע את התאריך.

---

## מוכן ואומת

| מה | מצב |
|---|---|
| **AAB חתום** | 8.3MB · `versionCode 1` · ריצה `33614636392` |
| keystore | `C:\Users\noam.edery\sportdle-keys\sportdle.keystore` · RSA 2048 · 27 שנה |
| 4 סודות ב-GitHub | `KEYSTORE_BASE64` · `KEYSTORE_PASSWORD` · `KEY_ALIAS` · `KEY_PASSWORD` |
| **App Links** | `assetlinks.json` מוגש בשורש, 200, `application/json`, **אפס הפניות**, טביעה תואמת |
| נכסי חנות | 7 קבצים ב-`store/upload/` |
| טקסטים | הכל ב-`tools/assets/store-listing.txt`, כולם בתוך המגבלות |
| פרטיות ותנאים | חיים על `sportdle.techbynoam.com` |
| iOS | הפרויקט קומפל בהצלחה על ראנר macOS |

---

## הרצף ביום האישור

### 1. יצירת האפליקציה  · ✅ **בוצע**
מזהה `4974294682160246747`. **`appId` = `com.techbynoam.sportdle`,
ואי אפשר לשנותו אחרי הפרסום הראשון.**

### 2. דף החנות  · *אני ממלא, אתה מאשר*
כל הטקסטים והנכסים מוכנים. שם, תיאור קצר, תיאור מלא, אייקון,
feature graphic, חמישה צילומים, קטגוריה, קישורים.

### 3. Data safety  · *אני ממלא*
חמשת הסעיפים ב-`store-listing.txt`. **מזהה פרסום = לא** כרגע —
לעדכן ביום שמוסיפים AdMob.

### 4. דירוג תוכן  · *אני ממלא*
**חייבים לסמן שיש תוכן שנוצר על ידי משתמשים** — שדה השם בלשונית
"קרב חברים". התעלמות מזה היא סיבה נפוצה לדחייה.

### 5. העלאת ה-AAB  · *ביחד*
מ-`Actions → AAB → artifacts`. אם אין artifact בתוקף (30 יום) —
מריצים שוב, `versionCode` יעלה לבד.

### 6. Play App Signing  · ✅ **בוצע**
Play App Signing פעיל, ולכן גוגל חותמת מחדש והטביעה שמותקנת אצל
המשתמשים היא **שלה**. `androidAppLinks.sha256` מכיל עכשיו שלוש
טביעות: המפתח הקלאסי של גוגל (`C2:2F`, זה שמגיע מהחנות), מפתח
ה-PQC שלה (`BA:AE`, בטא), ומפתח ההעלאה שלנו (`DD:32`, ה-APK
מגיטהאב). **הקובץ צריך לעלות לפרודקשן לפני הפרסום.**

**⚠ מלכודת שנתפסה:** באותו עמוד ב-Play Console יש קטע JSON מוכן
להעתקה, ו**הוא שגוי בשני שדות** — `package_name` מכיל את הדומיין
במקום את שם החבילה, והטביעה היא של מפתח **קודם** שהוחלף
(`90:41`, בסיס התקנות 0%). הטביעות הנכונות הן מלחצני ההעתקה
שליד "מפתח קלאסי" ו-"מפתח PQC". **לא מקטע ה-JSON.**

### 7. בדיקה סגורה  · *אני מגדיר, אתה מגייס*
**12 בודקים × 14 יום**, והשעון מתחיל כשהם מצטרפים בפועל. זה
הפריט שקובע את תאריך הפרסום. **כדאי שהרשימה תהיה מוכנה מראש.**

---

## שתי בדיקות במכשיר שעוד לא נעשו

APK עדכני: `Actions → APK`. הפוטר מציג `app 1.0.<מספר הריצה>` —
כך יודעים בוודאות איזו בנייה מותקנת.

| מה | אם נשבר |
|---|---|
| **קרב חברים** מול חבר אמיתי, דרך קישור הזמנה | ה-SDK של פיירבייס ב-WebView. סביר CSP או טעינת מודול |
| **אופליין** במצב טיסה | גופן שבור = `fonts.css` לא נכנס ל-`assets/public`. הקרב לא יעבוד — זה מכוון |

מה שכן נבדק ועובד: הקלדה, מחיקה, 561 דפי התוכן, שיתוף וואטסאפ,
רצף, והתראה יומית.


---

## מצב Play Console · 02/09/2026

**כל הגדרת האפליקציה הושלמה.** רשימת ההצהרות נעלמה מלוח השליטה
והמנעול שהיה על "בדיקות בקבוצות מוגדרות" נפתח — זה האישור
שאין יותר סעיף חסר.

| מה | מצב |
|---|---|
| דף האפליקציה בחנות | ✅ שם · תיאור קצר 50/80 · תיאור מלא 1261/4000 · סמל · feature · **5 טלפון + 5 טאבלט 7" + 5 טאבלט 10"** |
| קטגוריה ופרטי קשר | ✅ משחק → **טריוויה** · מייל · אתר · בלי טלפון (מוצג פומבית) |
| מדיניות פרטיות | ✅ `/privacy/` |
| פרטי הכניסה | ✅ "לא" |
| מודעות · מזהה פרסום | ✅ "לא" · "לא" |
| קהל היעד | ✅ 13-15, 16-17, 18+ |
| אבטחת נתונים | ✅ הוגש · פירוט למטה |
| **סיווג תוכן** | ✅ נשלח ל-IARC ב-2/9/2026 17:07 |
| ממשלתיות · פיננסיות · בריאות | ✅ שלוש "אין" |
| **AAB** | ✅ נבנה · `AAB #2` ריצה `33653568039` · 8.3MB · versionCode 2 · מ-`5ff9d8e` |
| **מסלול Alpha** | 🟡 מדינות ✅ (177) · כתובת משוב ✅ · **גרסה בטיוטה** `1.0.2 (2)` עם תוכן עדכון · **חסר: קובץ ה-AAB ורשימת בודקים** |
| **בדיקה סגורה** | ❌ 12 בודקים × 14 יום — **זה מה שקובע את התאריך** |

### הרצף שנשאר במסלול Alpha

מזהה המסלול: `4699459580637124005`. רשימת המשימות בעמוד שלו:

1. ✅ **בחר ארצות** — כל 177. נבחרו הכל בכוונה: האפליקציה בעברית
   אז אין חיסרון, וזה מונע חסימה של בודק שנמצא בחו"ל.
2. ❌ **בחירת בודקים** — צריך "רשימת אימיילים" עם הכתובות.
   **זה הפריט היחיד שתלוי באיסוף הכתובות.**
3. 🟡 **יצירת גרסה** — הטיוטה קיימת (`1.0.2 (2)`, תוכן עדכון
   בעברית עם רשימת מה לבדוק). **חסר רק הקובץ**: להוריד את
   `sportdle-aab` מריצה `33653568039` ולגרור ל-dropzone.
4. 🔒 תצוגה מקדימה ואישור
5. 🔒 שליחה לגוגל לבדיקה

**ה-AAB חייב להיות מהבנייה החדשה ולא מהראשונה.** דפי התוכן
ארוזים בתוך האפליקציה (`webDir: app-dist`), ולכן ה-AAB הראשון
(`1bc7492`) מכיל את נוסח הפרטיות הישן — זה שסותר את הצהרת
אבטחת הנתונים שהוגשה. `AAB #2` נבנה מ-`5ff9d8e` ומכיל את הנוסח
המתוקן.

### שתי מלכודות שנתפסו בדרך, ואסור לחזור אליהן

**1. השדה "אתר" בהגדרות החנות נשמר בשקט בלי להישמר.** הטופס
הציג "השינוי נשמר", המייל נכנס — והאתר נשאר ריק, ולכן המשימה
"קטגוריה ופרטי קשר" נשארה פתוחה בלי שום הודעת שגיאה. **תמיד
לפתוח מחדש את הדיאלוג ולקרוא את הערכים אחרי שמירה**, לא להסתמך
על הודעת ההצלחה.

**2. שאלון סיווג התוכן היה תקוע על "מתבצע" בגלל שלב 1, לא בגלל
השאלות.** אפשר לענות על כל 18 השאלות ולשמור, והשאלון עדיין לא
נשלח — כי תיבת ההסכמה ל-IARC בשלב הקטגוריה לא סומנה. ההודעה
היחידה שמגלה זאת היא "כדי להמשיך, עליך לאשר את התנאים
וההגבלות", בתחתית שלב 1.

### מה שהוצהר בסיווג התוכן
כל 18 השאלות "לא" — אלימות, פחד, מיניות, הימורים, שפה, סמים,
הומור גס, רכישות דיגיטליות, מיקום, סמלים נאציים, קוריאה, טרור,
פשעים — **חוץ משתיים**:
- **תקשורת/החלפת תוכן בין משתמשים = כן.** ב"קרב חברים" השם
  שמוקלד והניחושים נראים לשחקנים האחרים.
- **אינטראקציות מוגבלות למוזמנים בלבד = כן.** החדר נגיש רק עם
  הקוד או קישור ההזמנה.

ה"כן" הראשון פתח שלוש שאלות מודרציה, והתשובות לפי מה שקיים:
**אין חסימת משתמשים, אין דיווח על משתמשים, אין בקרת צ'אט** —
כי אין צ'אט. **אם IARC יעלה את דירוג הגיל בגלל אינטראקציה לא
מנוטרת, התיקון הוא להוסיף חסימה או דיווח**, ואז למלא שאלון חדש.

### מה הוצהר באבטחת נתונים, ולמה
נגזר מקריאת `engine.js` ו-`versus.js`, לא מהשערה:

| סוג שהוצהר | מאיפה בקוד | נדרש? | מטרה |
|---|---|---|---|
| **שם** | `players/{uid}/name` ב"קרב חברים", ושדה "איך לחזור אליכם?" | אופציונלי | תכונות + הודעות מהמפתח |
| **כתובת אימייל** | אותו שדה `#repFrom` — "שם או מייל" | אופציונלי | תכונות + הודעות מהמפתח |
| **תוכן אחר של משתמשים** | הטקסט החופשי ב-`type:"feedback"` | אופציונלי | תכונות |
| **פעולות אחרות** | `track()` — מועדון, מספר חידה, האם נפתר; וניחושים בחדר | **נדרש** (אין כיבוי) | תכונות + Analytics |
| **מזהה מכשיר/אחר** | `rid` ב-`type:"fix"`, ו-uid של פיירבייס | אופציונלי | תכונות + מניעת הונאות |

**לא הוצהר שיתוף עם צד שלישי:** פיירבייס ו-Apps Script הם התשתית
שלי, לא צד שלישי, והשם שמוצג לשחקנים אחרים בחדר הוא תצוגה
בתוך האפליקציה ולא העברה. מוצפן בהעברה = כן (שני היעדים `https`).
מחיקת נתונים = כן → `https://sportdle.techbynoam.com/contact/`

### למה דף הפרטיות שונה היום
השאלון אילץ לבדוק מה **באמת** יוצא מהמכשיר, ושתי אמירות בדף לא
היו מדויקות: המזהה `rid` הופיע כ"נשאר במכשיר" למרות שהוא נשלח
עם דיווח תיקון, ו"איני שומר נתונים אישיים בשרת" התעלם משדה
"שם או מייל" בטופס הדיווח. שני המקטעים תוקנו, ונוסף
`/contact/#delete` — **הכתובת שהוצהרה ל-Play כ"URL למחיקת
נתונים", עם התחייבות ל-30 יום.** אם הטקסט הזה ישתנה, ההצהרה
צריכה להשתנות איתו.

---

## תקלות שכבר נפתרו — לא לחזור אליהן

**`.well-known` בשורש היה 404.** השרת הסטטי של Railway מסתיר
קבצים שמתחילים בנקודה **בשורש בלבד** — `/.gitignore` גם הוא 404,
בעוד `/sportdle/.well-known/…` הוחזר 200. נפתר ב-**Rewrite Rule**
ב-Cloudflare, ולא ב-Redirect: אנדרואיד **אינו עוקב אחרי הפניות**
בקובץ הזה, ו-301 היה נכשל בשקט.

**`captureInput` שבר הקלדה.** מחליף את חיבור הקלט של ה-WebView
ב-`BaseInputConnection` גולמי; התווים מופיעים אבל `input` לא נורה.
**אין להחזיר.**

**`html5mode` שבר את כל 561 הדפים.** מגיש את `index.html` של
השורש לכל נתיב בלי נקודה. `tools/app-prepare.mjs` מוסיף
`index.html` מפורש. כיבוי `server.html5mode` היה מחזיר 404 במקום.

**נפילות CI שלא משתחזרות מקומית.** Capacitor 8 דורש **Node ≥ 22**,
ו-`gradlew` שנדחף מוינדוס חסר ביט הרצה. **לוגים של Actions דורשים
אימות** — לכן שלבים שעלולים ליפול מדפיסים ל-`::error::`, שנגיש
ב-API.

**מלכודת Groovy:** `versionName X ?: Y` נפרש כ-`(versionName(X)) ?: Y`.
סוגריים חובה.

**VPN של חברה אחרת חוסם את הדומיין.** אם משהו נראה שבור — **קודם
לנתק VPN, אחר כך לחשוד בקוד.**

---

## אסור לגעת

- **`androidScheme` / `hostname`** ב-`capacitor.config.json` — ה-origin
  של `localStorage`. שינוי מוחק לכל שחקן את הרצף.
- **`appId`** — לנצח אחרי הפרסום הראשון.
- **הגופנים המקומיים** — `build.mjs` מפיל בנייה שמחזירה קישור חיצוני.
- **הדומיין במניפסט** — מקודד שש פעמים ב-`intent-filter`, כי XML
  לא יכול לקרוא מ-JSON. החלפת דומיין דורשת לעדכן גם שם.

הרקע המלא: **[APP.md](APP.md)** · טקסטים: `tools/assets/store-listing.txt`
