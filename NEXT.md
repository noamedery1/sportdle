# מכאן ממשיכים

נכתב 01/09/2026, בסוף היום שבו ה-APK הראשון נבנה בהצלחה.
הברנץ' הוא **`app-capacitor`**. `main` לא נגעו בו.

**מצב:** APK של 10MB נבנה ב-GitHub Actions ומחכה לבדיקה במכשיר.
`Actions → APK → artifacts → sportdle-apk`.

---

## א. חוסם: הבדיקה במכשיר

חמישה דברים שעובדים בכל דפדפן ונשברים לפעמים ב-WebView. אין דרך
לאמת אותם בלי טלפון, ולכן הם ראשונים — כל מה שאחריהם עשוי להשתנות
לפי מה שיימצא.

| # | מה לבדוק | אם נשבר |
|---|---|---|
| 1 | **קרב חברים** — ה-SDK של פיירבייס נטען מ-gstatic בזמן ריצה | סביר שזה CSP או טעינת מודול ב-WebView. יידרש תיקון בטעינה |
| 2 | **כפתור וואטסאפ** — חייב לפתוח את וואטסאפ, לא דף בתוך האפליקציה | להוסיף `@capacitor/browser` או טיפול ב-`appUrlOpen` |
| 3 | **התראה יומית** — לאשר אחרי חידה, ולראות שהיא מגיעה ב-8:30 | לבדוק אם `repeats` בלי `allowWhileIdle` באמת נורה במכשיר |
| 4 | **רצף** — לשחק, לסגור לגמרי, לפתוח | אם אבד: ה-origin של ה-WebView השתנה. `androidScheme`/`hostname` |
| 5 | **אופליין** — מצב טיסה. המשחק והגופנים תקינים; הקרב לא יעבוד | גופן שבור = `fonts.css` לא נכנס ל-`assets/public` |

---

## ב. לא חוסם — אפשר לעשות מיד

### 1. למזג את הרווחים של האתר ל-`main`  ← הכי דחוף

שני שיפורים **שהאתר החי לא מקבל** כרגע, כי הם יושבים רק בברנץ':

* **גופנים מקומיים.** `main` עוד טוען מ-Google Fonts — נבדק:
  שני קישורים ב-`src/template.html`. זה גם תלות בצד שלישי וגם
  בקשה איטית בכל טעינה.
* **פרטיות ותנאים בנוסח סופי.** `main` עוד מציג את באנר
  "נוסח כטיוטה" — נבדק ונמצא. AdSense קורא את דף הפרטיות.

שתי אפשרויות: למזג את הברנץ' כולו ל-`main` (ואז `android/`
ו-Capacitor נכנסים גם, וזה בסדר — הם לא משפיעים על האתר), או
לקטוף רק את `dce5805` ואת שינויי הפרטיות. **המיזוג המלא פשוט
יותר ופחות מסוכן** מקטיפה חלקית של קובץ שנגעו בו פעמיים.

### 2. `versionCode` שמתקדם לבד

`android/app/build.gradle` קבוע על `versionCode 1`. גוגל פליי
**דוחה העלאה שנייה עם אותו versionCode**, וזו תקלה שמתגלה בדיוק
ברגע הלא נוח. לגזור מ-`github.run_number` או ממספר הקומיטים.

### 3. AAB חתום לחנות

ה-APK הנוכחי הוא `assembleDebug` — חתום במפתח הפיתוח של אנדרואיד
ולא מתאים לחנות. צריך:
* `bundleRelease` ב-workflow, מדולג בשקט כשאין סודות
* `KEYSTORE_BASE64`, `KEYSTORE_PASSWORD`, `KEY_ALIAS`, `KEY_PASSWORD`
  כסודות המאגר

**את ה-keystore יוצר בעל הפרויקט, לא אני.** זה אישור חתימה: מי
שמחזיק בו יכול לפרסם עדכונים בשם האפליקציה, ואיבוד שלו אומר
שאי אפשר לעדכן את האפליקציה לנצח. הפקודה:

```bash
keytool -genkey -v -keystore sportdle.keystore -alias sportdle \
  -keyalg RSA -keysize 2048 -validity 10000
```

לגבות אותו במקום שאינו המאגר.

### 4. צילומי מסך וגרפיקה לחנות

אפשר לייצר עם Playwright במידות שהחנויות דורשות, כמו
`tools/clip-hook.mjs` מייצר את הקליפ. גוגל דורשת גם **Feature
graphic 1024×500**. הרשימה של מה כדאי לצלם נמצאת ב-
`tools/assets/store-listing.txt`.

### 5. iOS

`npx cap add ios` + workflow על ראנר macOS. GitHub Actions **כן**
מקמפל, אבל `.ipa` להתקנה על מכשיר דורש חתימה — כלומר חשבון מפתח
אפל בתשלום, תעודה ופרופיל כסודות. בלי זה אפשר לבנות לסימולטור
בלבד, וזה עדיין שווה: הוא יתפוס בעיות קומפילציה מוקדם.

לא נוסף עדיין בכוונה — הוספה מפעילה ריצת CI חדשה, והעדפנו
שה-APK יגיע קודם.

---

## ג. על בעל הפרויקט, ואי אפשר להאציל

- [ ] **לקרוא את `/privacy/` ואת `/terms/`.** סימון הטיוטה הוסר
      לפי בקשה, והם בנוסח סופי — אבל שתי החנויות קוראות את דף
      הפרטיות בביקורת, ואני לא עורך דין.
- [ ] **חשבונות מפתח:** אפל 99$ לשנה, גוגל 25$ חד-פעמי.
- [ ] **12 בודקים ל-14 יום** — דרישה של גוגל פליי מחשבון אישי חדש
      לפני פרסום לייצור. **להתחיל לגייס במקביל, לא אחרי.** זה
      השלב שתוקע אנשים לחודש.
- [ ] **AdMob, לא AdSense.** AdSense אסור בתוך אפליקציה, והרצתו
      ב-WebView מסכנת את חשבון ה-AdSense עצמו. שני שילובים נפרדים.
- [ ] טפסי **Data safety** ו-**App Privacy** — התוכן מוכן
      ב-`tools/assets/store-listing.txt`.

---

## ד. מלכודות שכבר נשכנו בהן, כדי לא לחזור

**נפילות CI שלא משתחזרות מקומית.** שתיים היו, ושתיהן מאותה סיבה:
הראנר שונה מהמחשב. Capacitor 8 דורש **Node ≥ 22** (מקומית 24,
ב-CI היה 20; ההודעה היא "Please install the latest LTS version"
ולא אומרת במה מדובר), ו-`android/gradlew` נדחף מוינדוס בלי ביט
הרצה — מה שלא התגלה כאן כי **אין Java במחשב הפיתוח**, ולכן
Capacitor דילג על השלב לגמרי.

**לוגים של Actions דורשים אימות ואי אפשר לקרוא אותם מהסשן.** מה
שכן נגיש הוא annotations. לכן שלבים שעלולים ליפול מדפיסים את
השגיאה ל-`::error::` — זה מה שחשף את שתי הנפילות.

**שלושה דברים שאסור לגעת בהם:** `androidScheme`/`hostname`
(ה-origin של `localStorage` — שינוי מוחק לכל שחקן את הרצף),
`appId` (לנצח אחרי הפרסום הראשון), והגופנים המקומיים
(`build.mjs` מפיל בנייה שמחזירה קישור חיצוני).

הרקע המלא: **[APP.md](APP.md)**.
