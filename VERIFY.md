# איך מוודאים את הנתונים

הצינור בונה מאגר משלושה מקורות שלא מסכימים ביניהם. הקובץ הזה הוא
הסדר שבו בודקים אותם זה מול זה, מה רץ לבד ומה דורש אותך.

הכלל שלא משתנה: **הבדיקה לא כותבת למאגר.** היא מסמנת, ומכינה שורה
מוכנה להדבקה ל-`config/names-<slug>.json`. ההכרעה שלך.

---

## למה בכלל צריך

שלושת המקורות מודדים דברים שונים, וההבדל הוא לא רעש:

| מקור | מה הוא באמת אומר | מה חסר בו |
|---|---|---|
| ההתאחדות לכדורגל | מי נרשם בסגל, עונה־עונה | מ-2002/03 בלבד · אין עמדה |
| worldfootball | מי הופיע בסגל, 1969/70 ואילך | סגלים ישנים חלקיים |
| ויקיפדיה העברית | תקופת החוזה במועדון | לא לכל שחקן יש ערך |

אבי נמני נפל בדיוק בפער הזה: ההתאחדות הכירה שלוש עונות,
worldfootball לא גישר אליו בגלל תעתיק, וויקיפדיה ידעה את כל
ארבע־עשרה. הוא הופיע במשחק עם עונה ראשונה 02/03 ותואר אחד
במקום שמונה, עד שאוהד כתב.

ולכן גם: כשוויקיפדיה אומרת יותר מההתאחדות, זה **לא** אוטומטית
תיקון. שחקן שהושאל החוצה נשאר בוויקיפדיה רשום על המועדון. זה
ההבדל שהבדיקה מסמנת ולא מכריעה.

---

## הסדר

### 1. טבלת הקריירה מוויקיפדיה — אוטומטי, ~4 דקות

```bash
node scripts/scrape.mjs --source=wikicareer
```

קורא את `שנים כשחקן` ו-`מועדונים כשחקן` מתיבת המידע של כל ערך,
שומר רק את השורות של המועדון שלנו, וממיר לעונות: "1989–1998" הוא
89/90 עד 97/98. יוצא ל-`data/raw/<slug>-wikicareer.json`.

זה המקור היחיד שיודע מתי שחקן **התחיל**. רץ מול ה-API של ויקיפדיה,
בלי דפדפן, 50 ערכים בבקשה. אם תראה `429` — הוא ממתין ומנסה שוב לבד.

### 2. דפי השחקן של ההתאחדות — פותח דפדפן, ~שעה

```bash
node scripts/scrape.mjs --source=ifaplayers
```

**זה הצעד שמחזיר למשחק את מי שאין עליו נתונים.** דף הסגל נותן שם
ועונה בלבד; דף השחקן נותן **שנת לידה ואזרחות**. אין בו עמדה — אבל
שנת הלידה לבדה מפעילה את הגשר המבני מול worldfootball (לידה +
חפיפת עונות), ומשם מגיעה גם העמדה.

מה שצריך לדעת לפני שמריצים:

* **נפתח חלון כרום אמיתי.** ההתאחדות מאחורי Cloudflare, ו-`fetch`
  רגיל מקבל 403. אל תסגור אותו, ואפשר להמשיך לעבוד במקביל.
* **~2 שניות לשחקן**, כ-2,000 שחקנים. שעה בערך.
* **אפשר לעצור באמצע.** הקובץ נשמר כל 20 שחקנים, וההרצה הבאה
  מדלגת על מי שכבר נבדק. `--limit=100` לחתיכה קטנה.
* הפלט `data/raw/ifa-players.json` משותף לכל המועדונים: המפתח הוא
  השחקן, ומי ששיחק בשתיים מהן לא נשאב פעמיים.

אימות שהמספרים אמיתיים: `player_id=1426` מחזיר 04/1972, וויקיפדיה
כותבת 26 באפריל 1972.

### 3. בנייה מחדש — ושלוש הכרעות שצריך לעשות קודם

```bash
node scripts/enrich.mjs --ifaborn && node scripts/build.mjs
```

הדגל `--ifaborn` הוא בכוונה. בלעדיו `enrich` מתעלם מהקובץ, והמאגר
נבנה בדיוק כמו קודם. איתו הוא מזריק את שנות הלידה **לפני** שכבת
worldfootball, כי הגשר המבני שם נשען עליהן — וזה משנה הרבה:

| | בלי | עם |
|---|---|---|
| מוסתרים | 563 | **446** |
| שחקנים במשחק | 1,417 | **1,502** |
| בריכת התשובות | 522 | **531** |
| ממצאי "שנת לידה חסרה" בהצלבה | 469 | **13** |

ומה שדורש הכרעה לפני שמפעילים:

1. **שני "מוחמד גדיר"** — ילידי 1991 ו-2001, שתי רשומות בהתאחדות,
   שהמאגר איחד לאחת. הפיצול נכון; אבל חידה #21 של חיפהdle פורסמה
   בשם "מוחמד גדיר", וצריך להצביע על אחד מהם.
2. **שני "עמנואל בואטנג"** — ילידי 1996 ו-1997, אותו סיפור,
   חידה #57 של הפועלdle.
3. **"גרשון קופי" מול "גיימס ביסו"** — שתיהן חידות שפורסמו (#34
   ו-#76 של באר־שבעdle), ושתיהן מתמזגות לרשומה אחת. בערך של
   גרשון קופי בוויקיפדיה **אין** הפועל באר שבע בכלל, וההתאחדות
   מכירה רק את ביסו, ב-2011. כלומר אחת משתי החידות בנויה על
   שחקן שלא שיחק במועדון — ולא ברור איזו.

`enrich` נופל על כל אחת מהן עם הודעה מפורשת, ובכוונה: שם שכבר
פורסם לא משנה משמעות בשקט.

### 3ב. תשע חידות שהרמזים שלהן ישתנו

הפעלת הדגל מתקנת נתונים של תשעה שחקנים שכבר היו חידה. כולם
תיקונים, לא רגרסיות — למשל ניר רחמין, שהיה רשום **חלוץ ניגרי**
והוא **שוער ישראלי**, ומאור בוזגלו שאיבד עונה ותואר שהגיעו אליו
מרשומה של מיהאי קורהוט ההונגרי. הרשימה המלאה יוצאת מ-`git diff`
על `data/clubs/` אחרי הרצה עם הדגל.

### 4. ההצלבה

```bash
node tools/audit.mjs
```

עובר על כל שחקן מול כל מקור וכותב:

* `data/review/audit-<slug>.md` — דוח קריא, מקובץ לפי סוג הממצא
* `data/review/audit.csv` — שורה לכל ממצא, נפתח באקסל (יש BOM,
  אז העברית לא נשברת)

הממצאים:

| ממצא | פירושו | תיקון מוכן? |
|---|---|---|
| **המאגר קצר מדי** | ויקיפדיה מכירה עונות שאין לנו | כן |
| **המאגר ארוך מדי** | לנו יש עונות שוויקיפדיה לא מכירה | לא — כנראה השאלה |
| **סתירה** | כל צד מוסיף עונות שלשני אין | לא |
| **יש מקור, לא נכנס** | המקור יודע והצינור לא לקח | כן |
| **אין בשום מקור** | אף מקור לא יודע | לא — צריך עין אנושית |
| **שני ערכים באותו שם** | "גיא מלמד" 1979 ו-1992 | לא, לעולם |

תיקון מוכן נראה כך, ואפשר להדביק אותו כמו שהוא:

```json
"1426": { "spells": [[1990, 2003], [2006, 2008]] }
```

### 5. מה עוד חסר

```bash
node tools/hidden.mjs
```

כותב `data/review/hidden.md` — מי לא מוצג במשחק ומה חסר לו.

---

## מה נשאר ידני

**רענון worldfootball.** דפי הסגל מאחורי Cloudflare, ו-Playwright
נחסם אחרי הבקשה הראשונה. **לא נעשה ניסיון לעקוף זיהוי בוטים.**
מה שכן עובד: `node tools/urls.mjs` מייצר `*-wf-console.js`, פותחים
את worldfootball בדפדפן שלך, מדביקים בקונסול, והתוצאה נשלחת
ל-`scripts/serve.mjs` שכותב אותה ל-`data/raw/`. נדרש רק כשמוסיפים
עונה חדשה — הנתונים הקיימים כבר בקובץ.

**הכרעה בסתירות.** כל שורה ב-`audit.csv` שאין לה תיקון מוכן.
פותחים את הערך בוויקיפדיה, מסתכלים, ומחליטים. ההתאחדות היא
הסמכות לגבי "נרשם בסגל", ויקיפדיה לגבי "היה במועדון".

**שחקנים שאף מקור לא מכיר.** אחרי צעד 2 יישארו כאלה שאין להם
עמדה. `data/review/hidden.md` הוא הרשימה, והדרך היחידה למלא אותה
היא ידנית — ערך בוויקיפדיה שלא מקוטלג, אתר המועדון, או זיכרון.

---

## הכלל שמגן על מה שכבר פורסם

`enrich` נופל אם שם שנמצא ב-`config/schedule-<slug>.json` נעלם
מהמאגר. חידה שפורסמה לא משנה תשובה. תיקון עונות של שחקן שכבר היה
חידה משנה את הרמזים שלו — זה מותר ואפילו רצוי כשהנתון היה שגוי,
אבל שווה לדעת שזה מה שקורה.
